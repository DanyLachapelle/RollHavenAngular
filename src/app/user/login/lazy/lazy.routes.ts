// src/app/user/login/lazy/lazy.routes.ts
import { Routes } from '@angular/router';
import { LazyLoginComponent } from './lazy.component';

export const routes: Routes = [
  { path: '', component: LazyLoginComponent },
];
